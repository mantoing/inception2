version: "3"

services:
  nginx:
    build: req/nginx
    container_name: nginx
    ports:
      - 443:443
    restart: always
    volumes:
      - wp_volume:/var/www/html
    depends_on:
      - wordpress
    networks:
      - inception_network

  wordpress:
    build: req/wordpress
    container_name: wordpress
    restart: always
    volumes:
      - wp_volume:/var/www/html
    depends_on:
      - mariadb
    expose:
      - "9000"
    env_file:
      - .env
    networks:
      - inception_network

  mariadb:
    build: ./req/mariadb/
    container_name: mariadb
    restart: always
    expose:
      - "3306"
    volumes:
      - db_volume:/var/lib/mysql/
    env_file:
      - .env
    networks:
      - inception_network
      
volumes:
  db_volume:
  wp_volume:
     driver: local
     driver_opts:
       type: none
       device: /Users/jaeywon/inception2/inception/srcs/req/volume/wordpress
       o: bind

networks:
  inception_network:
    driver: bridge
  # inception2_network: